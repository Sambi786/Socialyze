--- src/components/SocialyzeCamera.tsx
+++ src/components/SocialyzeCamera.tsx
@@ -24,6 +24,7 @@
   const [selectedPreset, setSelectedPreset] = useState("Normal");
   const [showSliders, setShowSliders] = useState(false);
   const [capturedImage, setCapturedImage] = useState<string | null>(null);
+  const [capturedRawImage, setCapturedRawImage] = useState<string | null>(null);
   const [showSendModal, setShowSendModal] = useState(false);
   const [selectedFriend, setSelectedFriend] = useState<string | null>(null);
   const videoRef = useRef<HTMLVideoElement>(null);
@@ -66,7 +67,10 @@
       canvas.height = video.videoHeight || 800;
       const ctx = canvas.getContext("2d");
       if (ctx) {
+        ctx.filter = "none";
+        ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
+        setCapturedRawImage(canvas.toDataURL("image/jpeg"));
+
         ctx.filter = getFilterStyle(filterParams);
         ctx.drawImage(video, 0, 0, canvas.width, canvas.height);
         setCapturedImage(canvas.toDataURL("image/jpeg"));
@@ -80,6 +84,7 @@
         if (ctx) {
           ctx.fillStyle = `hsl(${filterParams.hue}, 50%, 50%)`;
           ctx.fillRect(0, 0, canvas.width, canvas.height);
+          setCapturedRawImage(canvas.toDataURL("image/jpeg"));
           ctx.fillStyle = "#fff";
           ctx.font = "bold 40px sans-serif";
           ctx.fillText("Virtual Snap", 200, 400);
@@ -101,6 +106,20 @@
     });
   };
 
+  const downloadImage = (dataUrl: string, filename: string) => {
+    const link = document.createElement('a');
+    link.href = dataUrl;
+    link.download = filename;
+    document.body.appendChild(link);
+    link.click();
+    document.body.removeChild(link);
+    toast({
+      title: "Saved to Gallery",
+      message: `Downloaded ${filename} successfully.`,
+      icon: "bell"
+    });
+  };
+
   const handleSendToFriend = () => {
     if (selectedFriend && capturedImage) {
       onSendToFriend(selectedFriend, `Sent a Snap!`);
@@ -126,8 +145,15 @@
             <X className="w-6 h-6" />
           </button>
           <div className="flex gap-2">
-            <button className="w-10 h-10 rounded-full bg-black/50 flex items-center justify-center text-white backdrop-blur-md">
-              <Download className="w-5 h-5" />
+            {capturedRawImage && (
+              <button onClick={() => downloadImage(capturedRawImage, 'snap-original.jpg')} className="px-3 h-10 rounded-full bg-black/50 flex items-center justify-center text-white text-xs font-bold backdrop-blur-md hover:bg-black/70">
+                <Download className="w-4 h-4 mr-1" /> Original
+              </button>
+            )}
+            <button onClick={() => downloadImage(capturedImage, 'snap-filtered.jpg')} className="px-3 h-10 rounded-full bg-black/50 flex items-center justify-center text-white text-xs font-bold backdrop-blur-md hover:bg-black/70">
+              <Download className="w-4 h-4 mr-1" /> Filtered
             </button>
           </div>
         </div>
